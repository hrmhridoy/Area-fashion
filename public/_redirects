# Netlify Redirects (_redirects file format)\n# Alternative to netlify.toml redirects\n# https://docs.netlify.com/routing/redirects/\n\n# HTTP to HTTPS (handled by Netlify automatically)\n# https://:splat redirects to https://:splat 301\n\n# API Routes - Don't redirect\n/api/* 200\n\n# Netlify Functions - Don't redirect \n/.netlify/functions/* 200\n\n# Static files\n/_next/* 200\n/public/* 200\n\n# Next.js catch-all (disabled - Next.js handles routing)\n# /* /index.html 200\n\n# Custom redirects\n/terms /legal/terms 301\n/privacy /legal/privacy 301\n/faq /help/faq 301\n\n# Old URLs redirect to new\n# /old-product /shop?search=new-product 301\n# /old-blog/* /blog/:splat 301\n